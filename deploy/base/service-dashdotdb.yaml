apiVersion: v1
kind: Service
metadata:
  labels:
    app: dashdotdb
  name: dashdotdb
spec:
  ports:
  - name: auth-proxy
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: dashdotdb
